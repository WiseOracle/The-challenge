const canvas = document.getElementById('starfield'); if(canvas){const ctx=canvas.getContext('2d');let stars=[];function init(){canvas.width=window.innerWidth;canvas.height=window.innerHeight;stars=[];for(let i=0;i<300;i++){stars.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,z:Math.random()*canvas.width});}}function animate(){ctx.fillStyle='black';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='white';for(let i=0;i<stars.length;i++){let s=stars[i];s.z-=2;if(s.z<=0){s.z=canvas.width;}let k=128.0/s.z,x=canvas.width/2+s.x*k,y=canvas.height/2+s.y*k;if(x>=0&&x<canvas.width&&y>=0&&y<canvas.height){let size=(1-s.z/canvas.width)*2;ctx.beginPath();ctx.arc(x,y,size,0,2*Math.PI);ctx.fill();}}requestAnimationFrame(animate);}window.onresize=init;init();animate();}